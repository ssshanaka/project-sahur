# Module 2 — Environment Setup

## Lesson 2.7 — TRL (Transformer Reinforcement Learning)

In **Lesson 2.6**, you learned how `peft` attaches lightweight LoRA adapters to frozen base models, cutting VRAM requirements by 75%+.

Now, we face the next practical question:

**How do we actually run training loops on LLM datasets?**

If you write a raw PyTorch training loop from scratch, you have to manually tokenize texts, handle variable-length conversations, pack sequences into GPU tensors, and ensure the loss function only evaluates the assistant's answers — not the user's question.

Enter **TRL (Transformer Reinforcement Learning)**.

TRL is Hugging Face's flagship library for **Supervised Fine-Tuning (SFT)** and **Preference Alignment (DPO/PPO/GRPO)**. It provides high-level trainers designed specifically for Large Language Models.

By the end of this lesson, you will understand how TRL connects datasets, tokenizers, PEFT adapters, and training loops into a unified fine-tuning pipeline.

---

# 🎯 Goal

By the end of this lesson, you will understand:

- What the `trl` library is and why standard PyTorch training loops are inadequate for LLMs
- The role and mechanics of **`SFTTrainer`** (Supervised Fine-Tuning)
- The concept of **Sequence Packing** and how it boosts GPU utilization
- The critical concept of **Loss Masking** (Response-Only Loss / `-100` target labels)
- The comparison between alignment algorithms: **SFT**, **DPO**, **PPO**, and **GRPO**
- Key training hyperparameters and the Effective Batch Size formula
- How TRL seamlessly connects `datasets`, `transformers`, and `peft` in **Project Sahur**

---

# Why Standard PyTorch Loops Fail for LLMs

In traditional machine learning — say, image classification — every input has a fixed shape ($224 \times 224$), and the target label is a single integer.

Conversational LLMs are fundamentally different:

```text
1. Variable Sequence Lengths ──► Conversations vary from 20 to 4,096 tokens
2. Multi-Turn Role Syntax    ──► Prompts contain System, User, and Assistant tags
3. Selective Loss Targets    ──► Loss MUST only be calculated on Assistant answers
```

Think about what happens if you train an LLM using a standard loss function across an entire conversation:

```text
User: "What is the capital of France?" → Assistant: "The capital of France is Paris."
```

The model spends 60%+ of its gradient updates learning to predict `"What is the capital of France?"` — the user's question! That teaches the model to generate user prompts rather than helpful answers.

It's like grading an actor on how well they recite stage directions instead of their actual lines.

TRL solves these problems with **Sequence Packing**, **Response-Only Loss Masking**, and specialized Trainers.

---

# Anatomy of ChatML Dataset Formatting

Before feeding data into TRL, conversations must be formatted into a standardized template.

Modern instruction models (like Qwen3) use **ChatML (Chat Markup Language)** to demarcate roles:

```json
{
  "messages": [
    {
      "role": "system",
      "content": "You are Sahur, a hyper-responsive Gen Z AI assistant."
    },
    {
      "role": "user",
      "content": "Yo Sahur, explain attention mechanisms in simple terms."
    },
    {
      "role": "assistant",
      "content": "No cap, attention is basically the model playing favorites with words!"
    }
  ]
}
```

When processed by the tokenizer, this becomes a raw string with special control tokens:

```text
<|im_start|>system
You are Sahur, a hyper-responsive Gen Z AI assistant.<|im_end|>
<|im_start|>user
Yo Sahur, explain attention mechanisms in simple terms.<|im_end|>
<|im_start|>assistant
No cap, attention is basically the model playing favorites with words!<|im_end|>
```

The tokenizer converts these role tags into special token IDs that the model recognizes as conversation boundaries. We'll work hands-on with chat templates in **Module 5 — Dataset Engineering**.

---

# Supervised Fine-Tuning with `SFTTrainer`

`SFTTrainer` is the primary training class in TRL. Think of it as an orchestra conductor that coordinates all the moving pieces of LLM training:

```text
┌────────────────────────────────────────────────────────────────────────┐
│ SFTTRAINER ENGINE                                                      │
├────────────────────────────────────────────────────────────────────────┤
│ Input Data   ──► Hugging Face Dataset (JSONL / ChatML conversations)   │
│ Formatting   ──► Automatic Prompt Formatting via Chat Templates        │
│ Packing      ──► Sequence Packing (Combines short inputs together)    │
│ Adapter      ──► Automatic peft LoRA Injection                        │
│ Output       ──► Trained Adapter Checkpoints                           │
└────────────────────────────────────────────────────────────────────────┘
```

### What `SFTTrainer` Does Automatically:

1. **Formats Conversations**: Parses raw conversation dictionaries into formatted ChatML strings.
2. **Packs Sequences**: Groups short conversations together to eliminate wasted padding tokens.
3. **Injects LoRA Adapters**: Accepts a `peft_config` and instantiates adapters automatically.
4. **Tracks Metrics**: Logs training loss, learning rate decay, and GPU memory to TensorBoard.

Without TRL, you'd have to write hundreds of lines of custom PyTorch code to handle all of this. With TRL, it's handled in a single `SFTTrainer(...)` call.

We'll write our actual training script in **Module 7 — Fine-Tuning**.

---

# Sequence Packing: Maximizing GPU Utilization

When fine-tuning on real conversations, sequence lengths vary widely:

- Conversation 1: 150 tokens
- Conversation 2: 300 tokens
- Conversation 3: 80 tokens

### Without Packing (Standard Padding):

All sequences are padded with dummy tokens up to `max_seq_length` (e.g. 2048):

```text
Sample 1: [ Tokens: 150 ] [ PADDING: 1898 zeroes... ]
Sample 2: [ Tokens: 300 ] [ PADDING: 1748 zeroes... ]
Sample 3: [ Tokens:  80 ] [ PADDING: 1968 zeroes... ]
```

Over **85%** of GPU computation is wasted on useless padding math!

### With Packing (`packing=True`):

`SFTTrainer` concatenates multiple short conversations into a single contiguous block of 2,048 tokens:

```text
Packed: [ Conv 1: 150 ] [ Conv 2: 300 ] [ Conv 3: 80 ] [ Conv 4: ... ] ► Total: 2048
```

Think of it like Tetris. Without packing, every block gets its own row. With packing, blocks are stacked tightly to fill every gap.

```text
Without Packing ──► ~ 15% Real Token Math, 85% Wasted
With Packing    ──► ~ 98% Real Token Math, 2% Wasted
```

Sequence packing increases effective training throughput by **2x to 4x**.

---

# Loss Masking: The Most Important Concept in SFT

This is one of the most critical ideas in instruction fine-tuning.

Remember our actor analogy from Module 0? The base model is a talented actor. The dataset is the script. During training, we want the model to practice delivering its _lines_ — not memorizing the stage directions.

In a conversation, the "stage directions" are the system prompt and user questions. The "lines" are the assistant's responses.

### The PyTorch `-100` Label Trick

PyTorch's `CrossEntropyLoss` has a built-in rule:

> Any token with a target label of **`-100`** is completely ignored during loss computation.

TRL uses this to create **Response-Only Loss Masking**:

```text
┌───────────────────────────────────────────────┬──────────────────────────────────────────────┐
│ PROMPT TOKENS (System & User)                 │ ASSISTANT RESPONSE TOKENS                    │
├───────────────────────────────────────────────┼──────────────────────────────────────────────┤
│ <|im_start|>user\nWhat is 2+2?<|im_end|>\n   │ <|im_start|>assistant\n4.<|im_end|>          │
│ Label Mask: -100 -100 -100 -100 -100 -100     │ Label Mask: 19  13  151645  (ACTIVE)         │
├───────────────────────────────────────────────┼──────────────────────────────────────────────┤
│ ❌ Loss Ignored                              │ ✅ Loss Computed & Gradients Backpropagated  │
└───────────────────────────────────────────────┴──────────────────────────────────────────────┘
```

Let's look at this token by token:

```text
┌────────┬──────────────────────────┬────────────────────────────────────────────┐
│ Index  │ Text Token               │ Target Label (Loss Mask)                   │
├────────┼──────────────────────────┼────────────────────────────────────────────┤
│ 0      │ <|im_start|>             │ -100 (Ignored)                             │
│ 1      │ user                     │ -100 (Ignored)                             │
│ 2      │ \n                       │ -100 (Ignored)                             │
│ 3      │ What                     │ -100 (Ignored)                             │
│ 4      │  is                      │ -100 (Ignored)                             │
│ 5      │  2+2?                    │ -100 (Ignored)                             │
│ 6      │ <|im_end|>               │ -100 (Ignored)                             │
│ 7      │ <|im_start|>             │ -100 (Ignored)                             │
│ 8      │ assistant                │ -100 (Ignored)                             │
│ 9      │ \n                       │ -100 (Ignored)                             │
│ 10     │ 4                        │ 19   (ACTIVE ✅)                           │
│ 11     │ .                        │ 13   (ACTIVE ✅)                           │
│ 12     │ <|im_end|>               │ 151645 (ACTIVE ✅)                         │
└────────┴──────────────────────────┴────────────────────────────────────────────┘
```

The model's gradients update **strictly on predicting the assistant's words**. This results in much sharper instruction-following and persona adherence.

Without loss masking, the model wastes training capacity learning to reproduce user questions. With loss masking, every gradient step is focused on improving the quality of Sahur's responses.

---

# Comparing Alignment Algorithms

TRL provides trainers for several post-training and alignment algorithms:

```text
┌───────────────────────────┬───────────────────────────────────┬────────────────────────────────┐
│ Algorithm                 │ Data Format Required              │ Complexity / Overhead          │
├───────────────────────────┼───────────────────────────────────┼────────────────────────────────┤
│ SFT (Supervised FT)       │ Single Prompts & Responses        │ Low (Fastest & simplest)       │
│ DPO (Direct Preference)   │ Pairs: Prompt, Chosen, Rejected   │ Medium (No Reward Model needed)│
│ PPO (Reinforcement Learn) │ Prompts + Environment Reward Model│ High (Requires 4 active models)│
│ GRPO (Group Relative PO)  │ Prompts + Rule Verification       │ Medium-High (Used in DeepSeek) │
└───────────────────────────┴───────────────────────────────────┴────────────────────────────────┘
```

For **Project Sahur**, we use **SFT** in Module 7 to teach the core Gen Z conversation format, then **DPO** in Module 8 to steer preference away from robotic boilerplate toward natural Gen Z responses.

### What's the difference?

Think of it this way:

```text
SFT  ➔ "Here's the script. Learn these lines."
DPO  ➔ "Here are two takes. This one is better. Learn the difference."
PPO  ➔ "Improvise, and I'll score your performance live."
```

SFT teaches the model _what_ to say. DPO teaches it to _prefer_ certain responses over others. We'll dive into DPO's mechanics in **Module 8 — Alignment & Behavior Tuning**.

---

# Mastering Training Hyperparameters

When configuring SFT training, you control several key parameters:

```text
┌───────────────────────────────┬───────────────────────────┬────────────────────────────────┐
│ Hyperparameter                │ Recommended Range         │ Explanation                    │
├───────────────────────────────┼───────────────────────────┼────────────────────────────────┤
│ learning_rate                 │ 1e-4 to 3e-4 (LoRA)       │ Higher rate for adapter tuning │
│ per_device_train_batch_size   │ 2 to 4                    │ Fits inside VRAM per GPU pass  │
│ gradient_accumulation_steps   │ 4 to 16                   │ Simulates large batch sizes    │
│ warmup_ratio                  │ 0.03 (3% of total steps)  │ Prevents initial gradient spike│
│ lr_scheduler_type             │ "cosine"                  │ Smooth learning rate decay     │
└───────────────────────────────┴───────────────────────────┴────────────────────────────────┘
```

### The Effective Batch Size Formula

Training stability improves with larger batch sizes — but fitting 32 sequences into a 4 GB GPU causes instant Out of Memory.

We solve this with **Gradient Accumulation**:

$$\text{Effective Batch Size} = \text{Batch Size} \times \text{Accumulation Steps} \times \text{GPUs}$$

For our setup: $2 \times 8 \times 1 = 16$ samples per step.

PyTorch processes 2 samples at a time in VRAM, accumulates gradients over 8 passes, and performs a single weight update at step 8. We get the stability of a 16-sample batch while only using 2.5 GB of VRAM.

Think of it like filling a swimming pool with a garden hose instead of a fire hydrant — same result, lower pressure.

### Learning Rate Warmup + Cosine Decay

```text
Learning Rate
  ▲
  │        /‾‾\
  │       /    \
  │      /      \
  │     /        \
  │    /          \__
  └────────────────────────► Training Steps
     Warmup (3%)  Cosine Decay (97%)
```

1. **Warmup (First 3%)**: Gradually ramps from zero to peak learning rate. Prevents massive gradient spikes from corrupting adapter weights.
2. **Cosine Decay (Remaining 97%)**: Smoothly decreases toward zero for stable convergence.

---

# Understanding Loss Curves

When training with TRL, the loss curve tells you how well the model is learning:

```text
Training Loss
  ▲
  │  ● (Step 0: Loss ~ 2.8)
  │   \
  │    \
  │     \__  ● (Step 100: Loss ~ 1.2)
  │        \___
  │            \__________  ● (Step 500: Loss ~ 0.45)
  └───────────────────────────────────► Training Steps
```

- **Step 0 to 50**: Rapid drop from ~2.8 to ~1.2 as the model learns ChatML formatting.
- **Step 50 to 300**: Steady decrease as the model absorbs persona vocabulary and style.
- **Overfitting Warning**: If training loss approaches 0.05 while eval loss starts rising, the model is memorizing training data word-for-word rather than learning patterns.

---

# How TRL Fits Into Project Sahur

Here is how all the tools connect:

```text
Hugging Face datasets ──► Formatted ChatML Conversations (sahur_genz_v1.jsonl)
                                  │
                                  ▼
Hugging Face peft      ──► Attaches LoRA Adapters (r=16, alpha=32)
                                  │
                                  ▼
Hugging Face trl       ──► SFTTrainer + Response-Only Loss Masking
                                  │
                                  ▼
Output Checkpoint      ──► 6 MB Adapter (checkpoints/sahur-sft-v1)
```

TRL acts as the high-level orchestration engine that ties data, model, and adapters into a clean, reproducible training run.

---

# Mental Model

Think of fine-tuning an LLM like coaching a professional actor:

```text
Base Model (Qwen3)   ➔ A Talented Actor (Knows grammar, facts, language)
Dataset              ➔ The Film Script (Dialogues & Persona)
PEFT / LoRA          ➔ The Character Makeup & Costume (Lightweight adaptation)
TRL (SFTTrainer)     ➔ The Director (Ensures they rehearse the lines, not the stage directions)
Loss Masking (-100)  ➔ Only scoring the actor on their actual performance, not the cue cards
```

---

# 🧠 Bootcamp Challenge

Test your knowledge without looking back:

1. Why does standard PyTorch training across an entire prompt string fail for instruction models?
2. What target label value does PyTorch `CrossEntropyLoss` ignore during loss computation?
3. How does Sequence Packing increase GPU training throughput by 2x–4x?
4. If `per_device_train_batch_size = 2` and `gradient_accumulation_steps = 8` on 1 GPU, what is the effective batch size?
5. What is the main structural difference between an SFT dataset and a DPO dataset?

---

# ⏭ Next Lesson — Module 2.8: Accelerate

Now that you understand how `SFTTrainer` manages training loops, how does Hugging Face dispatch model layers and gradients across different hardware — CPU, single GPU, or multi-GPU clusters?

In the next lesson, we will explore **Accelerate**:

- Hardware abstraction and why it matters
- The difference between DDP and FSDP
- How `device_map="auto"` works under the hood

See you in **Lesson 2.8**!
